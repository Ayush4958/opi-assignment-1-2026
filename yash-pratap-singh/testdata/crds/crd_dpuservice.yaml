# VALIDATION BOUNDARY: minimal stand-in schema for DPF's DPUService.
# group/version/kind and field shape are asserted from the DPF analysis doc,
# not confirmed against DPF source. Confirm before relying on them.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dpuservices.svc.dpu.nvidia.com
spec:
  group: svc.dpu.nvidia.com
  names:
    kind: DPUService
    listKind: DPUServiceList
    plural: dpuservices
    singular: dpuservice
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
